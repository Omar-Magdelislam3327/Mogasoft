import { NormalHtmlPipe } from './normal-html.pipe';

describe('NormalHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new NormalHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
